version: "3"

services:
  api:
    image: ghcr.io/engineer-man/piston
    container_name: piston_api
    restart: always
    privileged: true
    ports:
      - "2000:2000"
    volumes:
      - piston_data:/piston
      - /var/run/docker.sock:/var/run/docker.sock
    tmpfs:
      - /piston/jobs:exec

volumes:
  piston_data:
